package com.salil.dataStructures.linkedList;

import com.salil.dataStructures.linkedList.LinkedList;

/**
 * Created by B0097044 on 28/08/17.
 */
public class ZigZag {

/*

    public void zigZagList(LinkedList.Node head){
        boolean flag = true;
        LinkedList.Node current = head;
        LinkedList classObject = new LinkedList();
        while(current.next!=null){
            if(flag){
                if(current.data>current.next.data)
                    classObject.swapNodesByLink(current,current.next);//we can swap dat only of node without changing the link
                   // swap(current.data,current.next.data);
                }
                else{
                    if(current.data<current.next.data)
                        classObject.swapNodesByLink(current,current.next);
                    //swap(current.data,current.next.data);
                }
                current=current.next;
                flag=!flag;
            System.out.println("head : " + head.data);
            }
            classObject.printList();
    }

    public static void main(String[] args){
        // create a list 4 -> 3 -> 7 -> 8 -> 6 -> 2 -> 1
        // answer should be -> 3  7  4  8  2  6  1
        ZigZag ob = new ZigZag();
        LinkedList list = new LinkedList();
        list.append(4);
        list.append(3);
        list.append(7);
        list.append(8);
        list.append(6);
        list.append(2);
        list.append(1);
        ob.zigZagList(list.head);
        list.printList();

    }
*/

}



